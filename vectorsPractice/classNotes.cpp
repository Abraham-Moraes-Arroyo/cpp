#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std; 

int main(){
    vector<int> v1; // gives an empty vector 
    vector<int> v2(5);
    vector<int> v3(5,10);// five digits that are 10
    vector<int> v4 {7,3,8,6,4,5,2};// random vectors, you 
    // can always add more if you want.
    vector<string> v5{"MCS", "360", "at", "UIC"};
    
    
    return 0;
}