/*Print the ASCII table to screen, using the 
conversion char(i), for all integers i ranging from 
0 to 225*/
#include <iostream>
using namespace std;

int main(){
    char c[0,225];
    cout << "Enter a character: ";
    cin >> c;
    cout << "ASCII value of " << "is " << int(c);
    
    return 0;
}
// the example above takes the user's input and 
// converts it to ASCII value